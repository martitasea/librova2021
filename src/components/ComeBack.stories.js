import React from 'react';
import ComeBack from './ComeBack';

export default {
  title: 'SISTEMA/ComeBack',
  component: ComeBack,
};

const Template = args => <ComeBack {...args} />;

export const Default = Template.bind({});
Default.args = {
};